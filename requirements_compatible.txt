# Compatible requirements that work with existing environment
# These versions are chosen to avoid conflicts with your existing packages

# Core document processing (compatible versions)
opencv-python==4.8.1.78
Pillow==10.0.1
PyMuPDF==1.23.8
python-dateutil==2.8.2
pytesseract==0.3.10
pydantic==2.4.2

# Web framework (compatible versions)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
jinja2==3.1.2

# Avoid numpy conflicts by not specifying it
# Use your existing numpy version
